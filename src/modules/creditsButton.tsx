import { Tooltip } from "@/components/ui/tooltip";

interface CreditsButtonProps {
  credits: number;
}

export default function CreditsButton({ credits }: CreditsButtonProps) {
  return (
    <Tooltip>

    </Tooltip>
  );
}